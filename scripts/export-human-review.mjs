import fs from 'node:fs';import { OracleRuntime } from '../src/runtime.js';import { InMemoryStore } from '../src/memory/in-memory.js';const runtime=new OracleRuntime({mode:'production',memory:new InMemoryStore()});const prompts=['will sro win lane','is sro going to int','should sro queue one more','is this build troll','will jungle gank top'];const rows=[];for(let i=0;i<prompts.length;i++)rows.push(await runtime.answer({inquiry:prompts[i],debug:true,seed:`review-${i}`}));fs.writeFileSync('reports/V6_HUMAN_REVIEW.json',JSON.stringify(rows,null,2));console.log('reports/V6_HUMAN_REVIEW.json');
