# V4.9 Execution Brief

## Directive

Preserve the V4.9 product reasoning. Implement, test, or deploy it without flattening the routing hierarchy, response contracts, variety cycle, humor grammar, contextual emote meanings, target-affinity rules, or reviewed compound phrases.

## Runtime contract

1. Route recognition produces a canonical `route_family`.
2. Hard eligibility and reference permissions run before scoring.
3. Hierarchical specificity determines the candidate class.
4. Relevance admits a pool; it does not pick the winner.
5. Exact answers cycle without repetition until pool exhaustion.
6. Humor grammar changes the kind of retort before RNG.
7. The response's authored emote contract is merged with route context.
8. Target affinity and hard context blocks run before emote RNG.
9. Only a semantically compatible active-set alias may be appended.
10. Compound emote phrases must be reviewed, require every component alias, and remain rare.
11. Serious replies remain exempt from quota forcing.
12. No LLM or dynamic sentence generation is allowed at runtime.

## Before any change

Run:

```bash
npm run check
```

Do not weaken fixtures or audits to make a change pass. Do not promote daily banter into permanent lore. Return contradictions to the reasoning owner instead of guessing.

## Deployment

Do not change the public `/8ball` path. Configure Upstash-compatible Redis for shared cycles and locks. Sync the exact 7TV set ID before public deployment.
